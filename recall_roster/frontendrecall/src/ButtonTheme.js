import { createTheme, ThemeProvider } from '@mui/material/styles';

export const NavyTheme = createTheme({
    palette: {
      custom: {
        main: '#1c2347',
        contrastText: '#f1f1f1',
      },
    },
  });
  
